import { BackButton } from '@/components/common/BackButton';
import { CatalogGrid } from '@/components/collection/CatalogGrid';
const descriptions: Record<string, string> = { sarees: 'Handpicked silk, Banarasi, Ikat and celebratory drapes.', suits: 'Crafted suits for the most meaningful occasions.', kurtis: 'Light, elegant silhouettes for every kind of day.', dresses: 'Modern pieces with an unmistakably festive spirit.', lehengas: 'Statement lehengas that honour artistry and celebration.', 'indo-western': 'Contemporary forms with a heritage point of view.', 'co-ord-sets': 'Considered matching sets, ready for every plan.', jewellery: 'Finishing touches made to be remembered.', bags: 'Small occasion bags with a lot of character.', sale: 'A final opportunity to bring home a House of Aristocrat piece.' };
export default async function CollectionPage({ params }: { params: Promise<{ handle: string }> }) { const { handle } = await params; const title = handle.replaceAll('-', ' ').replace(/\b\w/g, (letter) => letter.toUpperCase()); return <main className="collection-page shell"><BackButton /><section className="collection-hero"><p className="eyebrow">The House of Aristocrat edit</p><h1>{title}</h1><p className="lede">{descriptions[handle] ?? 'A considered edit of pieces designed for the moments that become memories.'}</p></section><aside className="filter-bar"><span>Filters</span><button>Availability</button><button>Size</button><button>Colour</button><button>Price</button></aside><CatalogGrid collection={title} /></main>; }