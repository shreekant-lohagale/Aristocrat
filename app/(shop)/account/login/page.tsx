import { BackButton } from '@/components/common/BackButton';
import { getHostedCustomerAccountUrl } from '@/lib/shopify/customer-account-url';
export default function LoginPage() { const accountUrl = getHostedCustomerAccountUrl(); return <main className="auth-page"><div className="auth-back"><BackButton href="/" label="Back to Home" /></div><section className="auth-card"><p className="auth-wordmark">House of Aristocrat</p><p className="eyebrow">Private client access</p><h1>Welcome Back</h1><p>Access your orders, addresses and account.</p>{accountUrl ? <a className="google-sign-in" href={accountUrl}>Continue to sign in</a> : <p className="auth-error" role="alert">Secure customer access is managed by Shopify. Please contact us if the account link is unavailable.</p>}<p className="auth-coming-soon">Secure customer access is managed by Shopify.</p></section></main>; }
