# Features placeholder

[Back to the project README](../README.md)

There are no active feature modules in this folder. Current UI lives under `components/`, global browser state under `context/`, and server-side commerce logic under `lib/` and `app/` route handlers. Do not use this folder as evidence of a second architecture. If a future feature is moved here, update the root documentation and its import boundaries together.
