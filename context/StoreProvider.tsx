'use client';
import { createContext, useContext, useEffect, useMemo, useState } from 'react';
import type { CartLine, CatalogProduct, Country } from '@/types/commerce';
const countries: Country[] = [{ code: 'IN', name: 'India', flag: '🇮🇳', currency: 'INR', symbol: '₹', rate: 1 }, { code: 'US', name: 'USA', flag: '🇺🇸', currency: 'USD', symbol: '$', rate: .012 }, { code: 'CA', name: 'Canada', flag: '🇨🇦', currency: 'CAD', symbol: '$', rate: .016 }, { code: 'GB', name: 'United Kingdom', flag: '🇬🇧', currency: 'GBP', symbol: '£', rate: .0095 }, { code: 'AU', name: 'Australia', flag: '🇦🇺', currency: 'AUD', symbol: '$', rate: .018 }];
type Store = { country: Country; countries: Country[]; setCountry: (country: Country) => void; formatPrice: (price: number) => string; cart: CartLine[]; addToCart: (product: CatalogProduct, options?: Pick<CartLine, 'size' | 'color'>) => void; updateQuantity: (id: string, amount: number) => void; removeFromCart: (id: string) => void; cartCount: number; subtotal: number; wishlist: string[]; toggleWishlist: (id: string) => void };
const StoreContext = createContext<Store | null>(null);
export function StoreProvider({ children }: { children: React.ReactNode }) { const [country, setCountryState] = useState(countries[0]); const [cart, setCart] = useState<CartLine[]>([]); const [wishlist, setWishlist] = useState<string[]>([]); useEffect(() => { const saved = localStorage.getItem('mahera-country'); const savedCart = localStorage.getItem('mahera-cart'); const savedWishlist = localStorage.getItem('mahera-wishlist'); const found = countries.find((entry) => entry.code === saved); if (found) setCountryState(found); if (savedCart) setCart(JSON.parse(savedCart)); if (savedWishlist) setWishlist(JSON.parse(savedWishlist)); }, []); const setCountry = (value: Country) => { setCountryState(value); localStorage.setItem('mahera-country', value.code); }; const addToCart = (product: CatalogProduct, options: Pick<CartLine, 'size' | 'color'> = {}) => setCart((lines) => { const found = lines.find((line) => line.product.id === product.id && line.size === options.size && line.color === options.color); const next = found ? lines.map((line) => line === found ? { ...line, quantity: line.quantity + 1 } : line) : [...lines, { product, quantity: 1, ...options }]; localStorage.setItem('mahera-cart', JSON.stringify(next)); return next; }); const updateQuantity = (id: string, amount: number) => setCart((lines) => { const next = lines.flatMap((line) => line.product.id !== id ? [line] : line.quantity + amount > 0 ? [{ ...line, quantity: line.quantity + amount }] : []); localStorage.setItem('mahera-cart', JSON.stringify(next)); return next; }); const removeFromCart = (id: string) => setCart((lines) => { const next = lines.filter((line) => line.product.id !== id); localStorage.setItem('mahera-cart', JSON.stringify(next)); return next; }); const toggleWishlist = (id: string) => setWishlist((items) => { const next = items.includes(id) ? items.filter((item) => item !== id) : [...items, id]; localStorage.setItem('mahera-wishlist', JSON.stringify(next)); return next; }); const value = useMemo(() => ({ country, countries, setCountry, formatPrice: (price: number) => new Intl.NumberFormat(country.code === 'IN' ? 'en-IN' : `en-${country.code}`, { style: 'currency', currency: country.currency, maximumFractionDigits: country.currency === 'INR' ? 0 : 2 }).format(price * country.rate), cart, addToCart, updateQuantity, removeFromCart, cartCount: cart.reduce((sum, line) => sum + line.quantity, 0), subtotal: cart.reduce((sum, line) => sum + line.product.price * line.quantity, 0), wishlist, toggleWishlist }), [country, cart, wishlist]); return <StoreContext.Provider value={value}>{children}</StoreContext.Provider>; }
export function useStore() { const store = useContext(StoreContext); if (!store) throw new Error('useStore must be used inside StoreProvider'); return store; }
