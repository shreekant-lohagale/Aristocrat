'use client';
import { Heart, Minus, Plus, Star } from 'lucide-react';
import { useState } from 'react';
import type { CatalogProduct } from '@/types/commerce';
import { useStore } from '@/context/StoreProvider';
export function ProductDetails({ product, related }: { product: CatalogProduct; related: CatalogProduct[] }) { const { addToCart, formatPrice, wishlist, toggleWishlist } = useStore(); const [image, setImage] = useState(product.images[0]); const [size, setSize] = useState('M'); const [color, setColor] = useState(product.colors[0]); const [quantity, setQuantity] = useState(1); const saved = wishlist.includes(product.id); return <><section className="product-view"><div className="gallery"><div className="thumbnails">{product.images.map((entry) => <button key={entry} onClick={() => setImage(entry)}><img src={`/api/assets?file=${encodeURIComponent(entry)}`} alt="" /></button>)}</div><div className="main-image"><img src={`/api/assets?file=${encodeURIComponent(image)}`} alt={product.title} /></div></div><article className="product-info"><p className="eyebrow">{product.category}</p><div className="product-title-row"><h1>{product.title}</h1><button onClick={() => toggleWishlist(product.id)} aria-label="Save product"><Heart fill={saved ? 'currentColor' : 'none'} /></button></div><p className="rating"><Star size={14} fill="currentColor" /> {product.rating.toFixed(1)} · {product.reviewCount} reviews</p><div className="detail-price"><b>{formatPrice(product.price)}</b><s>{formatPrice(product.compareAtPrice)}</s></div><p className="lede">A considered Mahera piece, crafted in {product.fabric.toLowerCase()} and designed to make every occasion feel more meaningful.</p><p className="eyebrow">Colour — {color}</p><div className="swatches">{product.colors.map((entry) => <button key={entry} className={color === entry ? 'selected' : ''} onClick={() => setColor(entry)}>{entry}</button>)}</div><p className="eyebrow">Select size</p><div className="size-row">{product.sizes.map((entry) => <button key={entry} className={size === entry ? 'selected' : ''} onClick={() => setSize(entry)}>{entry}</button>)}</div><div className="detail-actions"><div className="quantity"><button onClick={() => setQuantity(Math.max(1, quantity - 1))}><Minus size={14} /></button><span>{quantity}</span><button onClick={() => setQuantity(quantity + 1)}><Plus size={14} /></button></div><button className="add-button" onClick={() => Array.from({ length: quantity }).forEach(() => addToCart(product, { size, color }))}>Add to bag</button></div><button className="buy-now" onClick={() => { addToCart(product, { size, color }); location.href = '/checkout'; }}>Buy now</button><details open><summary>Shipping & returns</summary><p>Complimentary delivery over ₹12,000. Returns accepted within 14 days of delivery.</p></details><details><summary>Fabric & care</summary><p>{product.fabric}. Specialist dry clean recommended.</p></details></article></section><section className="reviews"><p className="eyebrow">Client notes</p><h2>Reviews</h2><p>“Beautiful finishing and even more striking in person.” — Verified Mahera client</p></section></>; }
